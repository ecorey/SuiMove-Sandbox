module fp::oven {

    use sui::tx_context::{Self, TxContext};
    use std::string::{utf8, String};
    use sui::object::{Self, UID};
    use sui::package;
    use sui::display;

    use fp::flour::Flour;
    use fp::salt::Salt;
    use fp::yeast::Yeast;
    use fp::bread::Bread;

    // public fun create_bread(flour: Flour, salt: Salt, yeast: Yeast, ctx: &mut TxContext): Bread {
     
    // }
}